<%= form_for(@ticket_type) do |f| %>
  <% if @ticket_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_type.errors.count, "error") %> prohibited this ticket_type from being saved:</h2>

      <ul>
      <% @ticket_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<table>
		<tr>
			<th> <%= f.label 'Nazwa' %></th>
			<td> <%= f.text_field :name, :class => 'validate[required]' %></td>
		</tr>
		
		<tr>
			<th> <%= f.label 'Opis' %></th>
			<td> <%= f.text_area :description, :class => 'validate[required]' %></td>
		</tr>
	</table>  
  <br />
  <div class="actions">
    <%= f.submit 'Zapisz' %>
  </div>
<% end %>
