<h1>Lista pracowników</h1>
<%= link_to 'Dodaj', new_worker_path %><br><br>

<% if @workers -%>
	<table>
	  <tr>
	    <th>Id</th>
	    <th>Nazwa Kina</th>
	    <th>Status</th>
	    <th>Login</th>
	    <th>Hasło</th>
	    <th>Imie</th>
	    <th>Nazwisko</th>
	    <th>Telefon</th>
	    <th>E-mail</th>
	    <th></th>
	  </tr>
	<% @workers.each do |worker| %>
	  <tr>
	    <td><%= worker.id %></td>
	    <td><%= worker.cinema.name if worker.cinema %></td>
	    <td><%= worker.status.name if worker.status %></td>
	    <td><%= worker.login %></td>
	    <td><%= "*Hidden*" if worker.hashed_password %></td>
	    <td><%= worker.first_name %></td>
	    <td><%= worker.last_name %></td>
	    <td><%= worker.phone %></td>
	    <td><%= worker.email %></td>
	    <td><%= link_to 'Pokaż', worker %>
	    	<%= link_to 'Edytuj', edit_worker_path(worker) %>
	    	<%= link_to 'Usuń', worker, :confirm => 'Czy na pewno chcesz usunąć ten rekord?', :method => :delete %></td>
	  </tr>
	<% end %>
<% else -%>

	<p>Albo nie ma danych w bazie, albo masz niewystarczające uprawnienia, aby przeglądać te dane.</p><br><br>

<% end -%>

</table>

<br />

<%= link_to 'Dodaj', new_worker_path %>
