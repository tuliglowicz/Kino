<h1>Listing seances_types</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @seances_types.each do |seances_type| %>
  <tr>
    <td><%= seances_type.id %></td>
    <td><%= seances_type.type %></td>
    <td><%= link_to 'Show', seances_type %></td>
    <td><%= link_to 'Edit', edit_seances_type_path(seances_type) %></td>
    <td><%= link_to 'Destroy', seances_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Seances type', new_seances_type_path %>
