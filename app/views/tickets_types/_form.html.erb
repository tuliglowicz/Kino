<%= form_for(@tickets_type) do |f| %>
  <% if @tickets_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tickets_type.errors.count, "error") %> prohibited this tickets_type from being saved:</h2>

      <ul>
      <% @tickets_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.label 'Nazwa:' %><br />
    <%= f.text_field :name, :class => 'validate[required]' %>
  </div>
  <div class="actions">
    <%= f.submit 'Dodaj' %>
  </div>
<% end %>
