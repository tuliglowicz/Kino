<%= form_for(@seances_type) do |f| %>
  <% if @seances_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seances_type.errors.count, "error") %> prohibited this seances_type from being saved:</h2>

      <ul>
      <% @seances_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
